{
  "name": "daily-dua",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Daily Dua — static PWA with an optional Web Push reminder backend (Vercel serverless functions + KV).",
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "web-push": "^3.6.7"
  }
}
